eXT FRP Manager — Clean Source Export
======================================

This archive contains ONLY source code.

Included:
  ✅ frp_manager/         (Python package)
  ✅ tests/               (test suite)
  ✅ pyproject.toml       (dependencies)
  ✅ install.sh           (installer script)
  ✅ README.md            (documentation)
  ✅ LICENSE              (if present)

EXCLUDED (never included):
  ❌ data/                (state, tokens, configs)
  ❌ venv/                (virtual environment)
  ❌ __pycache__/ *.pyc   (compiled bytecode)
  ❌ *.bak *.bak.* *.current.*  (backups)
  ❌ *.ps1 *.bat *.cmd    (local scripts)
  ❌ .push_config.txt .gh_config.json  (local credentials)
  ❌ .git/                (version history)
  ❌ *.log                (runtime logs)

Installation on a new server:
  unzip frp-manager-clean-*.zip
  cd frp-manager
  sudo bash install.sh
  sudo frp-cli

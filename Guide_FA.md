# 🚀 راهنمای کامل eXtreme FRP Manager (v3.1)

**مدیر پیشرفته تونل FRP چندپروتکلی با تجمیع HAProxy**

> طراحی‌شده برای مسیریابی ترافیک ایران ← خارج با معماری Route/Channel

**فهرست مطالب**

- [بررسی سریع پروژه](#بررسی-سریع-پروژه)
- [ویژگی‌های کلیدی](#-ویژگی‌های-کلیدی)
- [صفحه ۱: معرفی و مفاهیم اصلی](#صفحه-۱-معرفی-و-مفاهیم-اصلی)
- [صفحه ۲: پیش‌نیازها و نصب](#صفحه-۲-پیش‌نیازها-و-نصب)
- [صفحه ۳: شروع سریع با ویزارد](#صفحه-۳-شروع-سریع-با-ویزارد-پیشنهادی)
- [صفحه ۴: منوی اصلی و میانبرها](#صفحه-۴-منوی-اصلی-و-میانبرها)
- [صفحه ۵: مدیریت Node، Route و Channel](#صفحه-۵-مدیریت-node-route-و-channel)
- [صفحه ۶: خروجی، Import و انتقال](#صفحه-۶-خروجی-import-و-انتقال-پروژه)
- [صفحه ۷: ابزارهای جانبی](#صفحه-۷-ابزارهای-جانبی-و-نگهداری)
- [صفحه ۸: عیب‌یابی](#صفحه-۸-عیب‌یابی-و-نکات-مهم)
- [لینک‌های مفید](#نقشه-راه-و-لینک‌های-مفید)

---

## بررسی سریع پروژه

این پروژه یک **مدیر پیشرفته تونل FRP** است که قابلیت‌های زیر را ارائه می‌دهد:

- پشتیبانی از پروتکل‌های **TCP، KCP، QUIC و WebSocket**
- تجمیع پهنای‌باند و Failover خودکار با **HAProxy**
- معماری **Hub / Node / Route / Channel**
- ویزارد ترکیبی (سریع و سفارشی)
- رابط کاربری دو زبانه (فینگلیش / انگلیسی)
- مدیریت کامل Node، Route و Channel
- تست سرعت، بهینه‌سازی، پشتیبان‌گیری و Export/Import

**نسخه:** v3.1  
**پلتفرم:** Ubuntu 22.04 / 24.04 و Debian 12+  
**نیازمندی:** Python 3.10+، دسترسی root، حداقل ۱ گیگابایت رم

**مخزن:** [https://github.com/URT19/eXT_FRP_Manager](https://github.com/URT19/eXT_FRP_Manager)

---

## ✨ ویژگی‌های کلیدی

| ویژگی | توضیح |
|-------|-------|
| **چندپروتکلی** | TCP (سریع‌ترین)، KCP (شبکه‌های پرپکت‌لاس)، QUIC (پهنای‌باند بالا)، WebSocket (دور زدن فایروال) |
| **تجمیع HAProxy** | توزیع ترافیک روی چند تونل موازی، Failover خودکار، بالانسینگ Round-robin / leastconn / source |
| **ویزارد ترکیبی** | ساخت Node + Route در یک جریان (حالت سریع: ۳ سؤال / حالت سفارشی: کنترل کامل) |
| **رابط کاربری** | باکس‌های رنگی، مشاور هوشمند، وضعیت زنده کانال‌ها، میانبرهای F-key |
| **راه‌اندازی آسان** | نصب یک‌کلیکی، خروجی تمیز پروژه، حذف کامل |

### موارد استفاده اصلی

1. **تجمیع پهنای‌باند**  
   یک اتصال کلاینت را روی چند سرور خارج توزیع کنید.

2. **مسیریابی چندمنطقه‌ای**  
   پورت‌های عمومی مختلف را به سرورهای مختلف هدایت کنید.

3. **دور زدن فایروال / DPI**  
   استفاده از WebSocket یا QUIC.

---

## صفحه ۱: معرفی و مفاهیم اصلی

### معماری پروژه

| اصطلاح | معنی |
|--------|------|
| **Hub** | سرور ایران (جایی که `frps` گوش می‌دهد) |
| **Node** | سرور خارج (جایی که `frpc` متصل می‌شود) |
| **Route** | یک پورت عمومی روی Hub که به یک یا چند کانال نگاشت می‌شود |
| **Channel** | یک تونل FRP تکی بین Hub و یک Node |

### حالت‌های Route

| حالت | تعداد کانال | HAProxy | کاربرد |
|------|-------------|---------|--------|
| **Simple** | ۱ | ✗ | ترافیک سبک، راه‌اندازی سریع |
| **Balanced** | ۲ تا ۸ | ✓ | پهنای‌باند بالا، High Availability، چند نود |

---

## صفحه ۲: پیش‌نیازها و نصب

### پیش‌نیازها

- سیستم‌عامل: Ubuntu 22.04 / 24.04 یا Debian 12+
- دسترسی root
- Python 3.10 یا بالاتر
- حداقل ۱ گیگابایت رم و ۱ هسته CPU

### نصب سریع (از Release)

```bash
wget https://github.com/URT19/eXT_FRP_Manager/releases/download/v3.1/frp-manager_v3.1.zip
unzip frp-manager_v3.1.zip
cd frp-manager
sudo bash install.sh
```

### نصب از گیت

```bash
git clone https://github.com/URT19/eXT_FRP_Manager.git
cd eXT_FRP_Manager
sudo bash install.sh
sudo frp-cli
```

> **نکته:** بعد از کلون، اگر پوشه `frp_manager` داخل مخزن وجود داشت، ممکن است نیاز باشد وارد آن شوید. ساختار Release معمولاً پوشه `frp-manager` است.

### گزینه‌های نصب

```bash
sudo bash install.sh                # نصب کامل (پیشنهادی)
sudo bash install.sh --cli          # فقط CLI
sudo bash install.sh --no-haproxy   # بدون HAProxy
sudo bash install.sh --upgrade      # ارتقا
sudo bash install.sh --uninstall    # حذف
```

### کارهای انجام‌شده توسط نصب‌کننده

- نصب وابستگی‌های سیستم (curl، iperf3، ufw، haproxy و ...)
- دانلود باینری‌های FRP v0.71.0 در `/usr/local/bin/`
- ساخت محیط مجازی پایتون در `/opt/frp-manager/venv`
- نصب پکیج پایتون
- ایجاد entrypointها:
  - `frp-manager`
  - `frp-cli`
  - `frp-tui`
  - `frp-export`
  - `frp-wizard`
  - `frp-uninstall`

### اجرا

```bash
sudo frp-cli           # منوی کلاسیک CLI
sudo frp-manager       # لانچر هوشمند
sudo frp-wizard        # مستقیم ویزارد
sudo frp-export        # خروجی ZIP تمیز
sudo frp-uninstall     # حذف نصب
```

---

## صفحه ۳: شروع سریع با ویزارد (پیشنهادی)

۱. دستور زیر را اجرا کنید:

```bash
sudo frp-cli
```

۲. گزینه **`[2] ویزارد (سریع)`** را انتخاب کنید.

۳. حالت سریع را انتخاب کرده و به سؤالات پاسخ دهید:

```text
گام ۱: انتخاب حالت سریع
گام ۲:
  نام Node: Frankfurt-A
  آدرس Node: 1.2.3.4
گام ۳:
  پورت Entry: 443
  پورت Target: 443
  پروتکل: 1 (TCP)
  حالت: 2 (Balanced)
گام ۴: تأیید ایجاد
```

### نتیجه ویزارد

- Node + Route + کانال‌ها ساخته می‌شوند
- فرانت‌اند HAProxy روی پورت Entry راه‌اندازی می‌شود
- همه سرویس‌های systemd فعال می‌شوند
- فایل compact برای Import روی Node آماده می‌شود

### حالت سفارشی

کنترل کامل روی:
- نام، آدرس، مکان و یادداشت Node
- پورت Entry و Target
- حالت Route (Simple / Balanced)
- ترکیب پروتکل‌ها (مثال: `tcp,tcp,ws`)

---

## صفحه ۴: منوی اصلی و میانبرها

### چیدمان منوی اصلی

```text
  [ 1]  نصب FRP v0.71.0
  [ 2]  ویزارد (سریع)                  ← پیشنهادی
  [ 3]  مدیریت Node-ها
  [ 4]  ساخت Simple Route
  [ 5]  ساخت Balanced Route
  [ 6]  مدیریت Route-ها
  [ 7]  خروجی برای Node
  [ 8]  ورودی روی Node
  [ 9]  مدیریت Channel-ها
  [10]  تست سرعت
  [11]  بهینه‌سازی
  [12]  پشتیبان‌گیری
  [13]  ریست
  [14]  زبان (فینگلیش / انگلیسی)
  [15]  راهنما (Help)
  [16]  حالت راهنما [ON/OFF]
  [17]  نمایش IP [ON/OFF]
  [18]  حذف نصب
  [ 0]  خروج
```

### میانبرهای صفحه‌کلید

| کلید | عملکرد |
|------|--------|
| **F1** | راهنمای کامل |
| **F2** | روشن/خاموش کردن حالت راهنما (نکات درون‌خطی) |
| **F3** | تغییر زبان (فینگلیش ↔ انگلیسی) |
| **F5** | تعویض مکان (ایران ↔ خارج) |
| **F7** | تغییر حالت پیشرفته / ساده منو |
| **F10** | خروج |
| **h** | راهنمای منوی فعلی |
| **0** | بازگشت / خروج |

---

## صفحه ۵: مدیریت Node، Route و Channel

### انواع پورت

| پورت | مکان | تولیدکننده | کاربرد |
|------|------|------------|--------|
| **Entry** | Hub | کاربر | پورت عمومی (کاربر اینجا وصل می‌شود) |
| **Bind** | Hub | خودکار (۴۰۰۰۰–۶۵۰۰۰) | `frps` اینجا گوش می‌دهد |
| **Remote** | Node | خودکار (۲۰۰۰۰–۲۹۹۹۹) | `frps` اینجا expose می‌کند |
| **Target** | Node | کاربر | سرویس نهایی (مثل Xray) اینجا گوش می‌دهد |
| **iPerf** | هر دو | خودکار (۵۵۰۰۰–۵۹۹۹۹) | تست سرعت |

### پروتکل‌ها

| پروتکل | انتقال | بهترین برای |
|--------|--------|-------------|
| **TCP** | TCP | سریع‌ترین و رایج‌ترین |
| **KCP** | UDP | شبکه‌های پرپکت‌لاس |
| **QUIC** | UDP | مدرن و پهنای‌باند بالا |
| **WebSocket** | TCP | دور زدن فایروال / CDN |

> **پیشنهاد برای Routeهای متوازن:** ترکیب `۲× TCP + ۱× WS` برای هر نود.

### جریان کار پیشنهادی

1. نصب FRP (`[1]`)
2. استفاده از ویزارد (`[2]`) یا:
   - افزودن Node (`[3]`)
   - ساخت Simple Route (`[4]`) یا Balanced Route (`[5]`)
3. خروجی برای Node (`[7]`)
4. Import روی Node (`[8]`)
5. مدیریت کانال‌ها (`[9]`)

---

## صفحه ۶: خروجی، Import و انتقال پروژه

### انتقال تنظیمات از Hub به Node

**روی سرور Hub (ایران):**

```bash
sudo frp-cli
# منوی [7] → انتخاب نود → کپی خروجی compact
```

**روی سرور Node (خارج):**

```bash
sudo frp-cli
# منوی [8] → چسباندن compact → خط خالی برای پایان
```

### خروجی تمیز کل پروژه

```bash
sudo frp-export
# خروجی: /root/frp-manager-clean-YYYYMMDD_HHMMSS.zip
```

فایل ZIP فقط شامل کد منبع است:
- ✅ `frp_manager/`، `pyproject.toml`، `install.sh`، `README.md`، `tests/`
- ❌ بدون `data/`، بدون `state.json`، بدون توکن، بدون IP، بدون لاگ، بدون venv

### انتقال به سرور جدید

```bash
# روی سرور قدیمی
sudo frp-export

# کپی به سرور جدید
scp /root/frp-manager-clean-*.zip root@NEW_SERVER:/root/

# روی سرور جدید
ssh root@NEW_SERVER
unzip frp-manager-clean-*.zip
cd frp-manager
sudo bash install.sh
sudo frp-cli
```

---

## صفحه ۷: ابزارهای جانبی و نگهداری

| گزینه منو | عملکرد |
|-----------|--------|
| **[10] تست سرعت** | تست با iperf3 |
| **[11] بهینه‌سازی** | تنظیمات سیستم و شبکه |
| **[12] پشتیبان‌گیری** | ذخیره state و کانفیگ‌ها |
| **[13] ریست** | پاک‌سازی (با احتیاط استفاده شود) |
| **[18] حذف نصب** | حذف کامل برنامه |

### دستورات مفید

```bash
sudo frp-cli          # منوی کلاسیک
sudo frp-manager      # لانچر هوشمند (سؤال CLI یا TUI)
sudo frp-wizard       # مستقیم ویزارد
sudo frp-export       # خروجی ZIP تمیز پروژه
sudo frp-uninstall    # منوی حذف نصب
sudo frp-tui          # TUI متنی (آزمایشی)
```

---

## صفحه ۸: عیب‌یابی و نکات مهم

### مشکلات رایج و راه‌حل

| مشکل | راه‌حل پیشنهادی |
|------|-----------------|
| سرویس‌ها بالا نمی‌آیند | بررسی وضعیت با `systemctl status` مربوط به frps / frpc / haproxy |
| کانال offline است | چک کردن فایروال (ufw)، پورت‌ها و اتصال شبکه بین Hub و Node |
| HAProxy کار نمی‌کند | مطمئن شوید با `--no-haproxy` نصب نکرده‌اید |
| مشکل زبان | با کلید **F3** یا منوی **[14]** تغییر دهید |
| پورت تداخل دارد | پورت‌های Bind و Remote خودکار انتخاب می‌شوند؛ در صورت نیاز Route را دوباره بسازید |

### نکات امنیتی و عملکردی

- همیشه با دسترسی **root** کار کنید.
- برای محیط تولید از ترکیب پروتکل‌های مختلف استفاده کنید.
- پشتیبان‌گیری منظم بگیرید (`[12]`).
- حالت راهنما (**F2**) را برای یادگیری روشن نگه دارید.
- از Export تمیز برای انتقال پروژه استفاده کنید تا داده‌های حساس منتقل نشوند.

### ساختار فایل‌ها پس از نصب

```text
/opt/frp-manager/
├── frp_manager/          # کد اصلی
├── venv/                 # محیط مجازی پایتون
├── data/                 # داده‌ها
│   ├── configs/
│   ├── logs/
│   ├── backups/
│   ├── exports/
│   └── haproxy/
└── ...
```

---

## نقشه راه و لینک‌های مفید

- **مخزن گیت‌هاب:** [https://github.com/URT19/eXT_FRP_Manager](https://github.com/URT19/eXT_FRP_Manager)
- **دانلود آخرین نسخه:** [Release v3.1](https://github.com/URT19/eXT_FRP_Manager/releases/download/v3.1/frp-manager_v3.1.zip)

---

> این راهنما بر اساس README رسمی پروژه و ساختار کد نوشته شده است.  
> آخرین به‌روزرسانی: ۱۴۰۵/۰۶/۲۵ (۱۶ سپتامبر ۲۰۲۶)
